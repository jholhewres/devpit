The instructions for this repository live in [AGENTS.md](AGENTS.md).

They are kept in one file rather than two so a rule cannot be right in one
place and stale in the other — the same reason `web/src/gen/` is generated
instead of hand-written.
