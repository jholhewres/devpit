[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
base64 = "0.22"
# The release manifest verifies every signature it publishes.
minisign-verify = "0.2.5"
serde_json = "1.0.151"
sha2 = "0.10"
walkdir = "2.5.0"

[dev-dependencies]
tempfile = "3.27.0"
