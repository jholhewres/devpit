# Controls with no handler, per file. One "path count" per line.
#
# Only ever goes down. A file not listed here is allowed none at all, which
# is the rule for anything written from here on. Regenerate with
# `cargo xtask controls` once controls have actually been wired.

